import React from 'react';

/*
* SearchResult component
* Renders a single search result
*
* @param {Any} props - Holds the object of the search result
* */
const SearchResult = () => (
    <div>
        <h1>
            SearchResult
        </h1>
    </div>
);

export default SearchResult;
